{
  "name": "imac-static-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "fetch": "node fetch-notion.mjs",
    "build": "node build.mjs",
    "preview": "npx http-server dist -p 8000 -c-1",
    "all": "npm run fetch && npm run build"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}